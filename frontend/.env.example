# Base URL of the Django backend API.
# Local dev: leave unset (Vite proxies /api to http://127.0.0.1:8000).
# Production: point to the deployed backend, e.g. https://your-backend.onrender.com
VITE_API_URL=